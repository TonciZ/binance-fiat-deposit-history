# Binance Full Deposit History Tool - Configuration Template
# Copy this file to .env and fill in your actual values

# =============================================================================
# REQUIRED: Binance API Credentials
# =============================================================================
# Get these from: https://www.binance.com/en/my/settings/api-management
# IMPORTANT: Only enable "Enable Reading" permission for security!

BINANCE_API_KEY=your_binance_api_key_here
BINANCE_API_SECRET=your_binance_secret_key_here

# =============================================================================
# OPTIONAL: Application Settings
# =============================================================================

# API call delay in milliseconds (default: 35)
# Lower values = faster fetching but higher rate limit risk
API_DELAY_MS=35

# Historical data fetch range (defaults: 2016 to current year + 1)
START_YEAR=2016
END_YEAR=2025

# Chart library to use (default: pyqtgraph)
# Options: "pyqtgraph" (recommended) or "matplotlib"
CHART_LIBRARY=pyqtgraph

# Preferred fiat currency for display (optional, auto-detected if not set)
# Example: EUR, USD, GBP, etc.
# PREFERRED_FIAT_CURRENCY=EUR
